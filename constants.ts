import {Difficulty} from './util/storage';

export const INIT_WORD_COUNT = 2;
export const INIT_DIFFICULTY: Difficulty = 'normal';
export const storage_key = {
  word_count: 'word-count',
  difficulty: 'difficulty',
};
